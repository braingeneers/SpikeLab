name: defaults
namespace: default
labels:
  app.kubernetes.io/managed-by: spikelab-nrp-jobs
affinity: {}
tolerations: []
default_secrets_mapping:
  s3_credentials_secret: prp-s3-credentials
resources:
  requests_cpu: "1"
  requests_memory: "2Gi"
  limits_cpu: "1"
  limits_memory: "2Gi"
  requests_gpu: 0
  limits_gpu: 0
  node_selector: {}
